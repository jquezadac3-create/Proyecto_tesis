server {
    listen 80;
    server_name barrabasazogues.com www.barrabasazogues.com 165.227.212.59;
    root /var/www/public;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://club.barrabasazogues.com$request_uri;
    }
}

server {
    listen 80;
    server_name club.barrabasazogues.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://club.barrabasazogues.com$request_uri;
    }
}

server {
    listen 443 ssl http2;

    server_name club.barrabasazogues.com;
    root /var/www/public;
    index index.php index.html;

    ssl_certificate /etc/letsencrypt/live/club.barrabasazogues.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/club.barrabasazogues.com/privkey.pem;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    client_max_body_size 25M;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
        gzip_static on;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
